server:
  port: 8761

spring:
  application:
    name: eureka-server

eureka:
  client:
    register-with-eureka: false   # 서버 자신은 등록 안 함
    fetch-registry: false         # 다른 레지스트리 정보 가져오지 않음
  server:
    enable-self-preservation: false  # 개발용 설정 (테스트용으로 false 가능)

